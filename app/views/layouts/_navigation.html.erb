<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">

    <%= link_to 'Menu', main_page_path, class: 'navbar-brand' %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <% if user_signed_in? %>
          <%# <li class="nav-item active">
            <a class="nav-link" href="#">Home <span class="sr-only">(current)</span></a>
          </li> %>
          <% if policy(:view).admin? %>
            <li class="nav-item active">
              <%= link_to 'workers', users_path, class: 'nav-link' %>
            </li>
          <% end %>
          <% if policy(:view).product_access? %>
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Products
              </a>
              <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                <%= link_to 'products', products_path, class: 'dropdown-item' %>
                <%= link_to 'product prices', product_prices_path, class: 'dropdown-item' %>
              </div>
            </li>
          <% end %>

          <% if policy(:view).access? %>
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="outcomerDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Clients
              </a>
              <div class="dropdown-menu" aria-labelledby="outcomerDropdown">
                <%= link_to "покупатели", outcomers_path(role: :покупатель), class: "dropdown-item" %>
                <%= link_to "поставщики", outcomers_path(role: :поставщик), class: "dropdown-item" %>
              </div>
            </li>
           <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="outcomerDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Add expenditure
              </a>
              <div class="dropdown-menu" aria-labelledby="outcomerDropdown">
                <%= link_to "for payment", new_expenditure_path(expenditure_type: 'зарплата'), class: "dropdown-item" %>
                <%= link_to "аванс", new_expenditure_path(expenditure_type: 'аванс'), class: "dropdown-item" %>
                <%= link_to "for the rest", new_expenditure_path, class: "dropdown-item" %>
              </div>
            </li>
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="outcomerDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                expenditures
              </a>
              <div class="dropdown-menu" aria-labelledby="outcomerDropdown">
                <%= link_to "for product", product_expenditure_expenditures_path, class: "dropdown-item" %>
                <%= link_to "payment", payment_expenditure_expenditures_path, class: "dropdown-item" %>
                <%= link_to "for the rest", expenditures_path, class: "dropdown-item" %>
              </div>
            </li>

          <% end %>
        <% end %>
      </ul>
    </div>
    <ul class="text-center navbar-nav ml-auto">
      <% if user_signed_in? %>
        <li class="nav-item ml-auto">
          <%= link_to "Выйти", destroy_user_session_path, class: "nav-link", data: { method: :delete, confirm: "Вы уверены?" } %>
        </li>
      <% else %>
        <li class="nav-item">
          <%= link_to "Войти", new_user_session_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Зарегистрироваться", new_user_registration_path, class: "nav-link" %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
